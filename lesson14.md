### 笔记

1. TCP：通道，握手（传输层），保证顺序和完整性。

- 每一次握手都要经过5层协议，由操作系统发起的
- 第一次：客户端发送给服务器，我想建立连接
  - SYN=1，表示发起并确定连接
  - seq=x，通信从x这个序列号开始
  - ACK=0，没有确认对方是否发过来
- 第二次：服务器接收并发送给客户端
  - SYN=1，发起确认连接
  - seq=y，序列号从y开始
  - ACK=1，服务器确认
- 第三次：客户端接收
  - SYN=0，不用发起了所以为0
  - seq=x+1
  - ACK=1，确认收到服务端的数据
  - ack=y+1

2. https TLS：握手在http之前，三次握手之后

- 加密传输，验证私钥
- TLS只有一次握手：
  - 告诉服务器版本，加密证书是什么（CA证书）
  - 客户端验证证书，是否真实，是否过期等
- openssl用来实现加密的方式，https是标准

3. 握手完成后，才开始发送真正的请求报文

### 课后作业

1. link标签 preload 手动预加载
2. 现代浏览器Pre-load Scanner
3. 增量渲染：一边解析一边渲染
4. http 1 2 3 有什么区别？

#### 项目

1. 收藏支持拖拽
2. 数据库设计，接入数据库
3. 修改prd：加入数据库设计
4. 技能中加入数据库 SQL
5. 技能中加入：https://prisma.org.cn/docs
